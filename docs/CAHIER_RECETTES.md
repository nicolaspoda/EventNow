# Cahier de recettes

Ci-dessous les 83 fonctionnalités de l'inventaire EventNow (13 domaines), du parcours simple aux règles métier les plus sensibles (accès, paiement, modération), plus une section de tests structurels et sécurité qui traverse tout le reste. Pour chaque ligne, j'ai vérifié la référence citée en lisant le code : nom de fichier de test et méthode/`describe` quand un test automatisé existe, ou un contrôle manuel direct sur l'API/le navigateur quand ce n'était pas le cas. Certaines lignes restent marquées "Non vérifié" : pas de test, pas eu le temps de vérifier à la main non plus, je préfère le dire clairement plutôt que deviner.

## Authentification & compte

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-01 | Fonctionnel | Inscription participant | POST /auth/register avec email/mdp valides | compte USER créé, tokens renvoyés | Inscription réussie, tokens délivrés (Automatisé : `auth.controller.spec.ts` / `auth.service.spec.ts` (`register`)) | **Conforme** |
| F-02 | Fonctionnel | Inscription organisateur | POST /auth/register-organizer | compte ORGANIZER créé | Compte organisateur créé (Automatisé : `auth.controller.spec.ts` (`registerOrganizer`) / `auth.service.spec.ts` (`registerOrganizer`)) | **Conforme** |
| F-03 | Fonctionnel | Connexion email/mot de passe | POST /auth/login avec credentials valides | tokens JWT renvoyés | Connexion réussie (Automatisé : `auth.controller.spec.ts` / `auth.service.spec.ts` (`login`) + Manuel (curl login admin/organizer/user/staff OK)) | **Conforme** |
| F-04 | Fonctionnel | Connexion via Google (OAuth) | Callback/exchange applicatif après redirection Google | utilisateur créé/lié, tokens renvoyés | Connexion Google fonctionnelle (Automatisé : `auth.service.spec.ts` (`validateGoogleUser`), `auth.controller.spec.ts` (`googleAuthCallback`, `googleExchange`)) | **Conforme** |
| F-05 | Fonctionnel | Rafraîchissement de session / déconnexion | POST /auth/refresh, /auth/logout | nouveaux tokens / token blacklisté | Session rafraîchie / déconnexion effective (Automatisé : `auth.controller.spec.ts` / `auth.service.spec.ts` (`refreshTokens`, `logout`)) | **Conforme** |
| F-06 | Fonctionnel | Consultation et édition de son profil | GET/PUT /auth/profile | profil renvoyé/mis à jour | Profil consulté et mis à jour correctement (Automatisé : `auth.controller.spec.ts` / `auth.service.spec.ts` (`getFullProfile`, `updateProfile`)) | **Conforme** |
| F-07 | Fonctionnel | Profil public d'un autre utilisateur | GET /auth/user/:userId/public-profile | profil public renvoyé (sans données privées) | Profil public renvoyé sans données privées (Automatisé : `auth.controller.spec.ts` / `auth.service.spec.ts` (`getUserPublicProfile`)) | **Conforme** |
| F-08 | Fonctionnel | Recherche d'utilisateurs | GET /auth/users/search?q=... | liste d'utilisateurs correspondants | Recherche fonctionnelle (Automatisé : `auth.controller.spec.ts` (`searchUsers`) / `auth.service.spec.ts` (`searchUsersByUsername`)) | **Conforme** |
| F-09 | Fonctionnel | Bannissement d'un utilisateur | PATCH /users/:id/ban (admin), GET /users/banned | utilisateur banni/débanni, listé | Bannissement / débannissement effectif (Automatisé : `users.service.spec.ts` (`banUser`, `getBannedUsers`) / `users.controller.spec.ts` + Manuel (curl admin ban/unban `user@eventnow.fr`)) | **Conforme** |

## Événements

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-10 | Fonctionnel | Parcourir / rechercher les événements | GET /events, /search, /suggestions, /categories, /locations, /cities | résultats filtrés/paginés | Recherche et filtres fonctionnels (Automatisé : `events.controller.spec.ts` / `events.service.spec.ts` (`findAll`, `searchEvents`, `getSearchSuggestions`, `getAvailableLocations`, `getAvailableCities`)) | **Conforme** |
| F-11 | Fonctionnel | Consulter le détail d'un événement | GET /events/:id | détail événement + note moyenne | Détail événement renvoyé correctement (Automatisé : `events.controller.spec.ts` / `events.service.spec.ts` (`findOne`)) | **Conforme** |
| F-12 | Fonctionnel | Créer un événement professionnel | POST /events, type PROFESSIONAL, par un Organisateur | événement créé | Événement professionnel créé, accès non-organisateur bloqué (Automatisé : `events.service.spec.ts` (`create` : création + rejet CLIENT sur événement PROFESSIONAL)) | **Conforme** |
| F-13 | Fonctionnel | Créer un événement communautaire | POST /events, type COMMUNITY, par un Participant ou Organisateur | événement créé | Événement communautaire créé (Automatisé : `events.service.spec.ts` (`create` : cas COMMUNITY pour CLIENT)) | **Conforme** |
| F-14 | Fonctionnel | Modifier un événement | PATCH /events/:id par le propriétaire | événement mis à jour | Modification réussie, accès non-propriétaire bloqué (Automatisé : `events.service.spec.ts` (`update` : succès + ForbiddenException si non-propriétaire)) | **Conforme** |
| F-15 | Fonctionnel | Annuler un événement | PATCH /events/:id/cancel | événement annulé, remboursements et notifications déclenchés | Annulation, remboursements et notifications déclenchés (Automatisé : `events.service.spec.ts` (`cancelEvent` : remboursements, notifications COMMUNITY vs PROFESSIONAL)) | **Conforme** |
| F-16 | Fonctionnel | Supprimer un événement | DELETE /events/:id par le propriétaire | événement supprimé | Suppression effective (Automatisé : `events.service.spec.ts` (`remove`)) | **Conforme** |
| F-17 | Fonctionnel | Suspendre un événement (modération) | PATCH /events/:id/suspend par un Admin | statut basculé SUSPENDED/ACTIVE | Suspension / réactivation effective, accès non-admin bloqué (Manuel : curl admin sur événement seedé (SUSPENDED confirmé puis restauré), 403 pour non-admin (aucune méthode `suspendEvent` couverte par un test automatisé)) | **Conforme** |
| F-18 | Fonctionnel | Statistiques de ventes d'un événement professionnel | GET /dashboard/organizer/events/:id/stats | statistiques de ventes par jour | Statistiques de ventes renvoyées (Automatisé : `dashboard.service.spec.ts` / `dashboard.controller.spec.ts` (`getEventStats`)) | **Conforme** |
| F-19 | Fonctionnel | Gérer la liste "quoi apporter" (événements communautaires) | GET/POST/PATCH/DELETE /events/:eventId/items, .../claim | items ajoutés/modifiés/réclamés, accès restreint | Liste gérée, accès restreint respecté (Automatisé : `event-items.service.spec.ts` / `event-items.controller.spec.ts` (accès via `getList`, `addItem`, `updateItem`, `deleteItem`, `claimItem`, claim et unclaim)) | **Conforme** |
| F-20 | Fonctionnel | Sondages internes à un événement (créer, voter, clôturer) | GET/POST /events/:eventId/polls, .../vote, .../close | sondage créé, votes enregistrés, clôture correcte | Sondage créé, voté et clôturé correctement (Automatisé : `polls.service.spec.ts` / `polls.controller.spec.ts` (`createPoll`, `vote`, `changeVote`, `closePoll`, `deletePoll`)) | **Conforme** |

## Participation & demandes (événements communautaires)

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-21 | Fonctionnel | Demander à participer à un événement communautaire | POST /participation-requests | demande PENDING créée, doublon refusé | Demande créée, doublon bloqué (Automatisé : `participation-requests.service.spec.ts` (`create`)) | **Conforme** |
| F-22 | Fonctionnel | Consulter ses demandes de participation | GET /participation-requests/my | liste des demandes de l'utilisateur | Demandes de l'utilisateur listées (Automatisé : `participation-requests.service.spec.ts` (`getMyRequests`)) | **Conforme** |
| F-23 | Fonctionnel | Gérer les demandes reçues (accepter/refuser) | GET .../pending-for-organizer, PATCH :id/respond | demande acceptée/refusée, stock décrémenté si applicable | Demande traitée, stock ajusté si besoin (Automatisé : `participation-requests.service.spec.ts` (`getPendingRequestsForOrganizer`, `respond`)) | **Conforme** |
| F-24 | Fonctionnel | Voir la liste des participants d'un événement communautaire créé | GET /dashboard/user/events/:id/participants (rôle USER) | liste des participants | Liste des participants renvoyée (Automatisé : `dashboard.controller.spec.ts` (`getEventParticipants`) + `dashboard.service.spec.ts` (`getEventParticipants` : agrégation bookings/tickets/requests)) | **Conforme** |
| F-25 | Fonctionnel | Mes événements à venir (tous types) | GET /dashboard/my-upcoming-events | événements futurs (pro + communautaires) | Événements à venir renvoyés (Automatisé : `dashboard.service.spec.ts` (`getMyUpcomingEvents`)) | **Conforme** |
| F-26 | Fonctionnel | Mes événements passés (participés) | GET /dashboard/my-participated-events | événements passés triés par date | Événements passés renvoyés (Automatisé : `dashboard.service.spec.ts` (`getMyParticipatedEvents`)) | **Conforme** |
| F-27 | Fonctionnel | Mon calendrier d'événements | GET /dashboard/my-calendar-events | liste combinée événements organisés/participés | Calendrier combiné renvoyé (Manuel : curl `user@eventnow.fr` : "Grande fête" renvoyé avec `participationType` correct (`dashboard.controller.spec.ts` couvre uniquement la délégation mockée vers le service, aucun test `dashboard.service.spec.ts` dédié à `getMyCalendarEvents`)) | **Conforme** |
| F-28 | Fonctionnel | Suivre / ne plus suivre un utilisateur | POST/DELETE /follows/user/:userId | relation follow créée/supprimée | Follow créé / supprimé (Automatisé : `follows.service.spec.ts` / `follows.controller.spec.ts` (`follow`, `unfollow`)) | **Conforme** |
| F-29 | Fonctionnel | Voir abonnés / abonnements / amis communs | GET .../followers, /following, /friends | listes renvoyées avec statut follow/ami | Abonnés, abonnements et amis renvoyés (Automatisé : `follows.service.spec.ts` (`getFollowers`, `getFollowing`, `getFriends`, `isFriend`)) | **Conforme** |
| F-30 | Fonctionnel | Activer/désactiver les notifications pour un follow | PATCH /follows/user/:userId/notifications | préférence mise à jour | Préférence de notification mise à jour (Automatisé : `follows.service.spec.ts` (`setNotificationsEnabled`)) | **Conforme** |

## Billetterie & réservation (événements professionnels)

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-31 | Fonctionnel | Réserver temporairement des billets (blocage 10 min) | POST /bookings | réservation PENDING créée, stock décrémenté, expiration 10 min | Réservation créée, stock décrémenté, expiration à 10 min (Automatisé : `bookings.service.spec.ts` (`createBooking`) + Manuel (`expiresAt` = +10 min confirmé via API)) | **Conforme** |
| F-32 | Fonctionnel | Consulter / confirmer / annuler une réservation en attente | GET /bookings, PATCH :id/confirm, DELETE :id | réservation listée/confirmée/annulée, stock restauré si annulation | Réservation listée / confirmée / annulée, stock restauré (Automatisé : `bookings.service.spec.ts` (`getUserBookings`, `confirmBooking`, `cancelBooking`, `expireOldBookings`)) | **Conforme** |
| F-33 | Fonctionnel | Payer et finaliser la commande (checkout) | POST /orders/payment/initiate puis /confirm | intention de paiement Stripe créée, commande confirmée | Paiement initié et commande confirmée (Automatisé : `orders.service.spec.ts` (`initiatePayment`, `confirmPayment`) + Manuel (booking payant → `clientSecret` Stripe test réel)) | **Conforme** |
| F-34 | Fonctionnel | Consulter ses commandes | GET /orders, /orders/:id | liste et détail de commande | Commandes listées et détail renvoyé (Automatisé : `orders.service.spec.ts` (`getUserOrders`, `getOrderById`) + Manuel (commande gratuite relue via `GET /orders/:id`)) | **Conforme** |
| F-35 | Fonctionnel | Confirmation de commande réussie | Affichage de `OrderSuccessPage` après paiement | page de confirmation avec récapitulatif | Page de confirmation affichée avec récapitulatif (Automatisé : `frontend/src/__tests__/OrderSuccessPage.test.tsx` (chargement, erreur backend, succès + récapitulatif de commande)) | **Conforme** |
| F-36 | Fonctionnel | Consulter et télécharger ses billets (QR code) | GET /tickets/my-tickets, /qr/:qrCode, /download/:id | billets listés, QR code lisible, PDF téléchargeable | Billets listés, QR lisible, PDF téléchargeable (Automatisé : `tickets.service.spec.ts` (`getUserTickets`, `getTicketByQRCode`) / `tickets.controller.spec.ts` (`getMyTickets`, `getTicketByQRCode`, `downloadTicket`) + Manuel (achat gratuit → `GET /tickets/download/:id` renvoie un PDF valide de 1 page)) | **Conforme** |
| F-37 | Fonctionnel | Appliquer un code promo à l'achat | POST /promo-codes/validate | réduction calculée, montant final renvoyé | Réduction appliquée correctement (Automatisé : `promo-codes.service.spec.ts` (`validatePromoCode`) + Manuel (code `RECETTE10` : -10 % sur 35 € → 31,50 €)) | **Conforme** |
| F-38 | Fonctionnel | Créer / lister / supprimer des codes promo pour un événement | POST /promo-codes, GET .../event/:eventId, DELETE :id | code créé/listé/désactivé, réservé à l'organisateur | Code promo créé / listé / supprimé (Automatisé : `promo-codes.service.spec.ts` (`createPromoCode`, `getEventPromoCodes`, `deletePromoCode`) + Manuel (cycle complet create/list/delete)) | **Conforme** |

## Paiement & remboursement (événements professionnels)

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-39 | Fonctionnel | Paiement en ligne (Stripe) | POST /orders/payment/initiate, /confirm, webhook Stripe | intention créée, commande confirmée après succès | Paiement Stripe initié et confirmé (Automatisé : `payment.service.spec.ts` (`createPaymentIntent`) / `orders.service.spec.ts` (`handleStripeWebhook`) + Manuel (`clientSecret` Stripe réel obtenu en mode test)) | **Conforme** |
| F-40 | Fonctionnel | Retour de paiement (redirection Stripe) | Redirection vers `PaymentReturnPage` après Stripe Checkout | statut de paiement affiché | Logique de la page de retour validée (Automatisé : `frontend/src/__tests__/PaymentReturnPage.test.tsx` (paramètres manquants, échec de chargement Stripe, confirmation + redirection vers order-success, écran de succès sans bookingId, erreur backend, écran "processing", échec de paiement) pour la logique de la page. Non vérifié pour le parcours Stripe Checkout complet avec carte 3-D Secure réelle, non exécutable dans le temps imparti) | **Conforme (page) / Non vérifié (parcours Stripe complet)** |
| F-41 | Fonctionnel | Demander un remboursement | PATCH /orders/:id/refund | commande en REFUND_REQUESTED, organisateur notifié | Remboursement demandé, organisateur notifié (Automatisé : `orders.service.spec.ts` (`requestRefund`)) | **Conforme** |
| F-42 | Fonctionnel | Approuver / refuser une demande de remboursement | GET /orders/refund-requests, PATCH :id/refund/approve, /reject | remboursement Stripe déclenché ou rejeté, utilisateur notifié | Remboursement approuvé / rejeté, utilisateur notifié (Automatisé : `orders.service.spec.ts` (`getRefundRequests`, `approveRefund`, `rejectRefund`)) | **Conforme** |

## Staff (contrôle d'accès événement, événements professionnels)

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-43 | Fonctionnel | Inviter un membre du staff pour un événement | POST /staff-invitations (Organisateur, événement PROFESSIONAL) | invitation créée, refusée si événement COMMUNITY | Invitation créée, refusée sur événement COMMUNITY (Automatisé : `staff-invitations.service.spec.ts` (`createInvitation` : création + rejet si événement non PROFESSIONAL)) | **Conforme** |
| F-44 | Fonctionnel | Accepter / décliner une invitation staff | GET /token/:token, POST /accept, /decline, GET /pending | statut de participation staff créé/refusé | Invitation acceptée / déclinée (Automatisé : `staff-invitations.service.spec.ts` / `.controller.spec.ts` (`acceptInvitation`, `declineInvitation`, `getInvitationByToken`)) | **Conforme** |
| F-45 | Fonctionnel | Révoquer une invitation staff | DELETE /staff-invitations/:id | invitation annulée par l'organisateur | Invitation révoquée (Automatisé : `staff-invitations.service.spec.ts` (`cancelInvitation`)) | **Conforme** |
| F-46 | Fonctionnel | Scanner les billets à l'entrée (QR code) | POST /tickets/validate | billet validé, doublon/annulé/hors-créneau détectés | Billet validé, cas d'erreur détectés (Automatisé : `tickets.service.spec.ts` (`validateTicket` : succès, ALREADY_VALIDATED, ORDER_CANCELLED, EVENT_NOT_STARTED)) | **Conforme** |
| F-47 | Fonctionnel | Consulter l'historique des validations d'entrée | GET /tickets/validations, /validations/stats | liste des scans + statistiques | Historique des scans et statistiques renvoyés (Automatisé : `tickets.service.spec.ts` (`getStaffValidations`, `getValidationStats`) + Manuel (curl `staff@eventnow.fr` sur "Daft Punk Tribute Night")) | **Conforme** |
| F-48 | Fonctionnel | Lister les événements où l'utilisateur est staff | GET /tickets/staff-events | liste des événements avec statut staff actif | Événements où l'utilisateur est staff renvoyés (Automatisé : `tickets.service.spec.ts` / `.controller.spec.ts` (`getStaffEvents`) + Manuel (curl `staff@eventnow.fr` → "Tech Summit Paris 2026", "Daft Punk Tribute Night")) | **Conforme** |

## Messagerie

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-49 | Fonctionnel | Consulter ses conversations | GET /messages/conversations | liste des conversations avec compteur de non-lus | Conversations listées avec compteur (Automatisé : `messages.service.spec.ts` (`getUserConversations`) / `messages.controller.spec.ts` (gestion de l'erreur P2021)) | **Conforme** |
| F-50 | Fonctionnel | Ouvrir/créer une conversation (privée ou liée à un événement) | POST /messages/conversations, GET /messages/events/:eventId/conversation | conversation DIRECT/GROUP/EVENT créée ou réutilisée | Conversation créée ou réutilisée (Automatisé : `messages.service.spec.ts` (`createConversation`, `getEventConversation`)) | **Conforme** |
| F-51 | Fonctionnel | Envoyer / lire des messages en temps réel | GET/POST /conversations/:id/messages, WebSocket `messages.gateway.ts` | message envoyé, diffusé en temps réel | Message envoyé et diffusé en temps réel (Automatisé : `messages.service.spec.ts` (`sendMessage`, `getMessages`) + `messages.gateway.spec.ts` (`handleConnection`, `handleJoinConversation`, `handleSendMessage`, notify methods)) | **Conforme** |
| F-52 | Fonctionnel | Gérer les membres d'une conversation de groupe | POST/DELETE .../members | membres ajoutés/retirés, conversation supprimée si vide | Membres ajoutés / retirés (Automatisé : `messages.service.spec.ts` (`addMembers`, `removeMember`)) | **Conforme** |
| F-53 | Fonctionnel | Renommer / supprimer une conversation | PUT/DELETE /conversations/:id | conversation renommée (GROUP) / supprimée par le créateur | Conversation renommée / supprimée (Automatisé : `messages.service.spec.ts` (`updateConversation`, `deleteConversation`)) | **Conforme** |
| F-54 | Fonctionnel | Marquer une conversation comme lue | POST .../read | compteur de non-lus remis à zéro | Compteur de non-lus remis à zéro (Automatisé : `messages.service.spec.ts` (`markAsRead`)) | **Conforme** |

## Avis & évaluations

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-55 | Fonctionnel | Laisser un avis sur un événement | POST /reviews/events/:eventId, GET .../can-review | avis créé uniquement si participation confirmée et événement terminé | Avis créé sous conditions respectées (Automatisé : `reviews.service.spec.ts` (`create` : COMMUNITY/PROFESSIONAL, non-participant refusé, doublon refusé)) | **Conforme** |
| F-56 | Fonctionnel | Consulter les avis d'un événement | GET /reviews/events/:eventId | avis paginés, email masqué, tri par note | Avis paginés et triés, email masqué (Automatisé : `reviews.service.spec.ts` (`findAllByEvent` : pagination, masquage email, tri)) | **Conforme** |
| F-57 | Fonctionnel | Modifier / supprimer son avis | PATCH/DELETE /reviews/:id | avis modifié/supprimé par son auteur uniquement | Avis modifié / supprimé par l'auteur uniquement (Automatisé : `reviews.service.spec.ts` (`update`, `delete`, ForbiddenException si non-auteur)) | **Conforme** |
| F-58 | Fonctionnel | Consulter ses propres avis laissés | GET /reviews/my-reviews | liste des avis de l'utilisateur | Avis de l'utilisateur listés (Automatisé : `reviews.service.spec.ts` (`findAllByUser`)) | **Conforme** |
| F-59 | Fonctionnel | Évaluer d'autres participants après un événement | POST /participant-reviews, GET .../participant/:id, .../event/:eventId | évaluation créée par l'organisateur uniquement, événement COMMUNITY terminé, participant accepté | Évaluation créée sous conditions respectées (Automatisé : `participant-reviews.service.spec.ts` (`create` : tous les cas de refus, création réussie)) | **Conforme** |
| F-60 | Fonctionnel | Modifier / supprimer une évaluation de participant | PATCH/DELETE /participant-reviews/:id | évaluation modifiée/supprimée par son auteur uniquement | Évaluation modifiée / supprimée par l'auteur uniquement (Automatisé : `participant-reviews.service.spec.ts` (`update`, `delete`, ForbiddenException si non-auteur)) | **Conforme** |

## Notifications

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-61 | Fonctionnel | Consulter ses notifications (avec compteur non lues) | GET /notifications, /unread-count | liste (filtrable non-lues) + compteur exact | Notifications listées avec compteur exact (Automatisé : `notifications.service.spec.ts` / `.controller.spec.ts` (`getForUser`, `getUnreadCount`)) | **Conforme** |
| F-62 | Fonctionnel | Marquer comme lue / tout marquer comme lu | PATCH :id/read, /read-all | statut lu mis à jour (une ou toutes) | Statut lu mis à jour (Automatisé : `notifications.service.spec.ts` (`markAsRead`, `markAllAsRead`)) | **Conforme** |
| F-63 | Fonctionnel | Supprimer une notification | DELETE /notifications/:id | notification supprimée, refusé si autre utilisateur | Notification supprimée, accès protégé (Automatisé : `notifications.service.spec.ts` (`delete`, ForbiddenException si autre utilisateur)) | **Conforme** |

## Modération & signalements

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-64 | Fonctionnel | Signaler un événement ou un utilisateur | POST /reports | signalement créé, doublon refusé, auto-signalement refusé | Signalement créé, doublon et auto-signalement bloqués (Automatisé : `reports.service.spec.ts` (`createReport` : validations, doublon P2002) + Manuel (signalement sur "Festival des Lumières")) | **Conforme** |
| F-65 | Fonctionnel | Consulter ses propres signalements | GET /reports/my | liste des signalements de l'utilisateur | Signalements de l'utilisateur listés (Automatisé : `reports.service.spec.ts` (`getMyReports`)) | **Conforme** |
| F-66 | Fonctionnel | Consulter tous les signalements et traiter leur statut | GET /reports, PATCH :id/status (Admin) | liste complète renvoyée, statut mis à jour | Liste complète renvoyée, statut mis à jour (Manuel : curl admin : `GET /reports` renvoie le signalement créé, `PATCH :id/status` (RESOLVED) confirmé (aucun test automatisé sur ces deux endpoints ; seuls `createReport`/`getMyReports` sont couverts)) | **Conforme** |

## Tableaux de bord

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-67 | Fonctionnel | Dashboard générique (redirection selon rôle) | Accès à `/dashboard` | redirection `DashboardRedirectPage` vers `/dashboard/organizer`, `/dashboard/user` ou `/events` selon le rôle | Redirection correcte selon le rôle (Automatisé : `frontend/src/__tests__/DashboardRedirectPage.test.tsx` (non authentifié → /events, ORGANIZER → dashboard organisateur, USER → dashboard participant, autre rôle → /events)) | **Conforme** |
| F-68 | Fonctionnel | Dashboard organisateur (vue d'ensemble événements professionnels) | GET /dashboard/organizer/overview, /organizer/events | statistiques et événements PROFESSIONAL de l'organisateur | Vue d'ensemble organisateur renvoyée (Automatisé : `dashboard.service.spec.ts` (`getOrganizerOverview`, `getOrganizerEvents`) + Manuel (curl `organizer@eventnow.fr` : 2 événements PROFESSIONAL)) | **Conforme** |
| F-69 | Fonctionnel | Dashboard participant (vue d'ensemble événements communautaires créés) | GET /dashboard/user/overview, /user/events | statistiques et événements COMMUNITY créés par le participant | Vue d'ensemble participant renvoyée (Automatisé : `dashboard.service.spec.ts` (`getClientOverview`, `getClientEvents`) + Manuel (curl `user@eventnow.fr`)) | **Conforme** |

## Divers

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| F-70 | Fonctionnel | Upload d'images (événements, avatar) | POST /upload/image, /images, DELETE /upload/image | image(s) uploadée(s) sur Cloudinary, validation type/taille, suppression | Image(s) uploadée(s), validation et suppression OK (Automatisé : `upload.service.spec.ts` / `.controller.spec.ts` (`uploadImage`, `uploadMultipleImages`, `validateImageFile`, `deleteImage`)) | **Conforme** |
| F-71 | Fonctionnel | Géocodage d'adresses (recherche de lieu à la création d'événement) | Appel `geocodingService.ts` → API adresse.data.gouv.fr | suggestions d'adresses avec coordonnées GPS | Suggestions d'adresses avec coordonnées GPS renvoyées (Automatisé : `frontend/src/__tests__/geocodingService.test.ts` (`searchAddress`, `reverseGeocode`, requête courte/vide, réponse mappée, erreur réseau, réponse non-ok)) | **Conforme** |
| F-72 | Fonctionnel | Rappels par email automatiques avant un événement | Tâche planifiée `email-reminders.job.ts` (J-7, J-1) | emails de rappel envoyés, agrégés par commande | Rappels envoyés et agrégés par commande (Automatisé : `email-reminders.job.spec.ts` (`sendReminders7Days`, `sendReminders1Day`)) | **Conforme** |
| F-73 | Fonctionnel | Nettoyage automatique des statuts staff expirés | Tâche planifiée `staff-cleanup.job.ts` | statuts staff supprimés pour les événements terminés | Statuts staff expirés nettoyés (Automatisé : `staff-cleanup.job.spec.ts` + `tickets.service.spec.ts` (`removeStaffForEndedEvents`)) | **Conforme** |
| F-74 | Fonctionnel | Envoi d'emails transactionnels | Backend `mail` (confirmation commande, rappels J-7/J-1, annulation, email de test) | email envoyé avec le bon contenu, échec géré sans interrompre le flux | Email envoyé avec le bon contenu (Automatisé : `mail.service.spec.ts` / `.controller.spec.ts` (`sendOrderConfirmation`, `sendEventReminder7Days`, `sendEventReminder1Day`, `sendEventCancellation`, `sendTestEmail`) + Manuel (log "Email confirmation envoyé à user@eventnow.fr" lors de l'achat gratuit du domaine 4). Note : l'invitation staff passe par une notification in-app (`notifications.service.ts`) et non par le module `mail`) | **Conforme** |

## Tests structurels et sécurité

| ID | Type | Fonctionnalité | Scénario | Résultat attendu | Résultat obtenu | Statut |
|---|---|---|---|---|---|---|
| S-01 | Structurel | Réservation avec stock insuffisant | POST /bookings avec quantité > stock disponible | exception levée, aucune écriture en base | Exception levée, aucune écriture en base (Automatisé : `bookings.service.spec.ts` (`createBooking`, `should throw BadRequestException if insufficient stock`)) | **Conforme** |
| S-02 | Structurel | Stock épuisé pendant la transaction (race condition) | Le verrou Redis passe mais la mise à jour du stock en transaction échoue (stock déjà épuisé par une réservation concurrente) | exception levée | Exception levée en cas de stock épuisé pendant la transaction (Automatisé : `bookings.service.spec.ts` (`createBooking`, `should throw if stock depleted during transaction`)) | **Conforme** |
| S-03 | Structurel | Expiration des réservations en attente | Job `expireOldBookings` sur une réservation PENDING dont `expiresAt` est dépassé | réservation passée EXPIRED, stock restauré | Réservation expirée, stock restauré (Automatisé : `bookings.service.spec.ts` (`expireOldBookings`, `should expire old pending bookings and restore stock`)) | **Conforme** |
| S-04 | Structurel | Génération d'un QR code valide et lié à un billet unique | Génération de l'image QR à partir de la valeur stockée sur le billet | image QR valide | Image QR générée correctement ; unicité par billet non couverte (Automatisé (partiel) : `tickets/qr-generator.service.spec.ts` (`generateQRImage`) couvre la génération de l'image. La génération de la valeur unique par billet (méthode privée `generateQRCode` dans `orders.service.ts`, appelée par `confirmPayment`) n'est couverte par aucun test dédié. `orders.service.spec.ts` mocke directement `ticket.create` avec un `qrCode` fixe (`'QR1'`)) | **Non vérifié pour le volet unicité : test à ajouter (assertion que deux billets d'une même commande reçoivent des `qrCode` distincts)** |
| S-05 | Structurel | Validation des données à la création d'un événement | POST /events avec titre/lieu manquant, date invalide, type/catégorie invalide, ou `ticket_categories` vide | requête rejetée (400) | Requête rejetée sur données invalides (Automatisé : `events/dto/create-event.dto.spec.ts` (`should reject a missing title`, `should reject a missing location`, `should reject an invalid event_date`, `should reject an invalid type`, `should reject an invalid category`, `should reject an empty ticket_categories array`)) | **Conforme** |
| SEC-01 | Sécurité | Hashage des mots de passe (jamais stocké en clair) | Inscription d'un utilisateur | mot de passe haché via bcrypt avant persistance | Mot de passe haché avant persistance (Automatisé : `auth/auth.service.spec.ts` (`register`, `should successfully register a new client user` : vérifie l'appel `bcrypt.hash(password, 10)` avant `user.create`)) | **Conforme** |
| SEC-02 | Sécurité | Protection XSS (contenu avec balise `<script>` neutralisé) | Corps de requête contenant `<script>alert("xss")</script>` | tags neutralisés avant traitement | Balises `<script>` neutralisées (Automatisé : `common/interceptors/sanitize.interceptor.spec.ts` (`should sanitize HTML from string body`, `should sanitize nested objects`) + `security/security.spec.ts` (`should sanitize XSS attempts`)) | **Conforme** |
| SEC-03 | Sécurité | Rate limiting (6 tentatives de connexion en moins de 60s → la 6ᵉ rejetée en 429) | 6 appels à POST /auth/login en moins de 60s | le 6ᵉ appel renvoie 429 | Rate limiting déclenché en conditions réelles (Automatisé (config) : `security/rate-limit.config.spec.ts` teste la configuration (`should be defined`, `should be a module`) sans exercer le comportement + Manuel : rafale de requêtes déclenchée en local sur /auth/login, événement `RATE_LIMIT` confirmé dans les logs de sécurité (capture conservée, dossier principal p.21). Aucun test automatisé (unitaire ou e2e) n'exerce ce comportement dans le repo) | **Conforme** |
| SEC-04 | Sécurité | Contrôle d'accès (endpoint admin appelé sans authentification → 401) | Appel d'un endpoint protégé par `JwtAuthGuard`/`RolesGuard` sans token | requête rejetée | 401 renvoyé sans authentification (Automatisé (niveau guards) : `auth/guards/jwt-auth.guard.spec.ts` et `auth/guards/roles.guard.spec.ts` testent la logique des guards sur un `ExecutionContext` mocké + Manuel : requête POST sans token sur /api/v1/mail/test via Postman, 401 "Unauthorized" confirmé (capture conservée, dossier principal p.29)) | **Conforme** |
